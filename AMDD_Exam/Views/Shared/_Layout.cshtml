<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - AMDD Skills Exam</title>
    <link rel="icon" type="image/png" href="~/favicon.png" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-dark amdd-navbar mb-3" style="padding:0 1rem;">
            <div class="container-fluid d-flex align-items-center" style="height:56px;">
                <!-- Left: logo + name -->
                <a class="navbar-brand fw-bold d-flex align-items-center gap-2 mb-0"
                   asp-controller="Exam" asp-action="Index"
                   style="font-size:1rem;line-height:1;">
                    <img src="~/favicon.png" alt="AMDD"
                         style="width:26px;height:26px;filter:brightness(0) invert(1);flex-shrink:0;" />
                    AMDD Skills Exam
                </a>
                <!-- Center: subtitle -->
                <span class="flex-grow-1 text-center small d-none d-md-block"
                      style="color:#d9b8ff;letter-spacing:0.01em;">
                    Branch Banking Application Development Unit
                </span>
                <!-- Right: reviewer button -->
                <a asp-controller="Exam" asp-action="ReviewerLogin"
                   class="btn btn-sm"
                   style="background:var(--amdd-600);color:#fff;border:none;
                          font-size:0.78rem;padding:5px 14px;border-radius:6px;
                          white-space:nowrap;flex-shrink:0;">
                    🔍 Reviewer
                </a>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer mt-4" style="background:var(--amdd-900);">
        <div class="container py-2 small" style="color:var(--amdd-400);">
            &copy; @DateTime.Now.Year — Branch Banking Application Development Unit &mdash; Internal Use Only
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
